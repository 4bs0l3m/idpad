/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AdsrPipe } from './adsr.pipe';

describe('Pipe: Adsre', () => {
  it('create an instance', () => {
    let pipe = new AdsrPipe();
    expect(pipe).toBeTruthy();
  });
});
